version: 2.1

jobs:
  build:
    docker:
      - image: circleci/ruby:2.7

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: |
            gem install bundler
            bundle install

      - run:
          name: Run tests
          command: |
            bundle exec rspec
