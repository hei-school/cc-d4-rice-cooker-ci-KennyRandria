version: 2.1

jobs:
  build:
    docker:
      - image: google/dart:latest

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: dart pub get

      - run:
          name: Run tests
          command: dart test
